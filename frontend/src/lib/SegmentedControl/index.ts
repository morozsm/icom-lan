/**
 * SegmentedControl — selector-family primitive
 *
 * Library-stable name for the segmented selector component.
 * This is the intended public API name (see radio-ui-control-system-plan.md §6.2).
 *
 * Production panels migrated to this entry point in #345.
 */
export { default as SegmentedControl } from '../../components-v2/controls/SegmentedButton.svelte';
